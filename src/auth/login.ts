// import { User } from "../types/user";

// // import { fakeFetchSession } from "../repositories/sessionRepository";
// import { fakeFetchUserByEmail } from "../repositories/userRepository";
// import { getOrCreateSession } from "./session";

// export async function fakeLogin(email, passport):Promise<User | null> { 
//     try {
//         const user = await fakeFetchUserByEmail()
        
//     } catch( err ) {
//         const error = err instanceof Error ? err : new Error('Unknow error during login')
//         console.error(error)
//         return null 
//     }
// }
